app:
  port: 3000

database:
  host: localhost
  port: 5432
  username: postgres
  password: postgres
  database: weather_activity_planner

openMeteo:
  baseUrl: https://api.open-meteo.com/v1

scheduler:
  enabled: true
  batchSize: 20